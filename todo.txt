                                RVSP button functionality
                                    get id through query
                                    write to id
                                    multiple names/record?
                                add field to DB for rsvp'd
                                add conteacts to db (script)
                                change attending/+1 order
                                confirmation page
                                handle alternate names/ nicknames
                                on submit only if form filled / errors


mobile
improve animations
same first/last name? Stan vs jan/stan
handle if already rsvp'd - cookie?
timing for +1 / btn
add db console page (think clients)
no plus one if name changed
#NEXT 3 : MODAL
add map
add address/info
where registered
